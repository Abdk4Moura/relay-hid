[package]
name = "relay-desktop"
version = "0.1.0"
edition = "2021"
description = "Relay WiFi receiver — injects keyboard/mouse input from the Relay phone app via Linux uinput"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
evdev = "0.12"

[profile.release]
strip = true
opt-level = "z"
